% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_sldsc_cont.R
\name{run_sldsc_cont}
\alias{run_sldsc_cont}
\title{Run S-LDSC Pipeline (hg38) with a Custom \emph{Continuous} BED + Baseline v2.2}
\usage{
run_sldsc_cont(
  polyfun_path,
  ldsc_path,
  sumstats_path,
  n,
  trait,
  onekg_path,
  bed_dir,
  baseline_dir,
  frqfile_pref,
  hm3_snps,
  out_dir
)
}
\arguments{
\item{polyfun_path}{A character string specifying the directory containing the PolyFun scripts
(e.g., \code{ldsc.py}, \code{munge_polyfun_sumstats.py}).}

\item{ldsc_path}{A character string specifying the directory containing the \code{ldsc.py} script
(not actually used here for annotation, but you may store related LDSC scripts in the same place).}

\item{sumstats_path}{A character string specifying the directory with the cleaned summary
statistics file \code{<trait>_sumstats.txt.gz}.}

\item{n}{Integer. The number of samples in the GWAS.}

\item{trait}{A character string representing the GWAS trait (e.g., \code{"SIM"}).}

\item{onekg_path}{A character string specifying the prefix to 1000 Genomes Plink files in hg38,
e.g. \code{"/path/1000G.EUR.hg38."}. Each chromosome file is then
\code{1000G.EUR.hg38.1.bed/.bim/.fam}, etc.}

\item{bed_dir}{A character string specifying the directory containing the user-supplied \strong{continuous} BED
file(s) in hg38. Only the first matching \code{*.cont.bed} found is used to create the annotation
for \strong{each} chromosome. (You can adjust \code{pattern} if you want a different naming scheme.)}

\item{baseline_dir}{A character string specifying the prefix to baseline v2.2 annotations for LDSC,
e.g. \code{"/path/baselineLD_v2.2/baselineLD."}.}

\item{frqfile_pref}{A character string specifying the prefix to allele frequency files in hg38,
used by LDSC (e.g. \code{"/path/1000G.EUR.hg38."}).}

\item{hm3_snps}{A character string specifying the path to the HapMap3 SNP file
(e.g. \code{"hm3_no_MHC.list.txt"}).}

\item{out_dir}{A character string specifying the output directory for LDSC results.
The function will create:
\itemize{
\item \code{"annotations/<trait>"} for annotation files
\item \code{"results"} for final LDSC logs/results
}}
}
\value{
\strong{No object} is returned. All intermediate files (\code{<trait>.<chr>.annot.gz},
\code{<trait>.<chr>.l2.ldscore.gz}, etc.) are written to \code{out_dir/annotations/<trait>},
and the final LDSC results (\code{.log} and \code{.results} files) to \code{out_dir/results}.
}
\description{
This function runs the S-LDSC pipeline using PolyFun scripts for:
\enumerate{
\item Munging summary statistics (\code{"munge_polyfun_sumstats.py"}),
\item Generating continuous annotations from a user-supplied BED file (hg38) via an R function
\code{\link{create_continuous_annotation}},
\item Computing LD scores (\code{ldsc.py --l2}),
\item Performing final heritability estimation (\code{ldsc.py --h2})
by combining the custom annotation(s) with baseline v2.2 annotations.
}
}
\details{
\strong{Step-by-step}:
\enumerate{
\item \strong{Munge summary stats} \code{<trait>_sumstats.txt.gz} into a parquet file with
\code{munge_polyfun_sumstats.py}.
\item For each chromosome \code{1..22}:
\itemize{
\item \strong{Create a continuous annotation} (via \code{\link{create_continuous_annotation}})
from the single user-supplied BED file (found in \code{bed_dir}) plus the
\code{.bim} for that chromosome.
\item \strong{Compute LD scores} (\code{ldsc.py --l2}) for this annotation.
}
\item Finally, \strong{run S-LDSC} (\code{ldsc.py --h2}) referencing both the newly created annotation
(\code{out_dir/annotations/<trait>/<trait>.<chr>}) and \code{baseline_dir},
providing \code{frqfile_pref} for the allele frequency files and the \code{hm3_snps}
for SNP filtering.
}
}
\examples{
\dontrun{
run_sldsc_cont(
  polyfun_path = "/path/polyfun",
  sumstats_path= "/path/sumstats",
  n            = 60000,
  trait        = "SIM",
  onekg_path   = "/path/1000G.EUR.hg38.",
  bed_dir      = "/path/my_bedfiles", # containing e.g. "k4_annotations_cont.bed"
  baseline_dir = "/path/baselineLD_v2.2/baselineLD.",
  frqfile_pref = "/path/1000G.EUR.hg38.",
  hm3_snps     = "/path/hm3_no_MHC.list.txt",
  out_dir      = "/path/k4_annotations_cont_results"
)
}

}
